[Config]
# All 13 known vala-lint checks listed explicitly (error or off).

block-opening-brace-space-before = error
double-semicolon = error
double-spaces = error
ellipsis = off
line-length = error
max-line-length = 100
ignore-line-length-comments = true
naming-convention = off
no-space = error
note = off
space-before-paren = error
trailing-newlines = error
trailing-whitespace = error
unnecessary-string-template = error
use-of-tabs = error
